# window scrollTo and scrollBy method
[JS-FILE](/js/89-window-scrollTo-and-scrollBy-method.js)

```javascript
function scrollTo(){
    window.scrollTo(20,50);
}

function scrollBy(){
    window.scrollBy(20,50);
}
```